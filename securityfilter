public boolean isSymmetric(String s) {
    if (s == null || s.isEmpty()) return false;
    
    char first = s.charAt(0);
    char last = s.charAt(s.length() - 1);
    
    return first == last;
}
// isSymmetric("aba") -> true
// isSymmetric("abc") -> false
